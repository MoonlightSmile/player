import WaveSurfer from "wavesurfer.js";

export default class MyPlayer extends WaveSurfer {
  constructor(options: any) {
    super(options);
  }

  public getSrc() {
    return super.getSrc();
  }
}
